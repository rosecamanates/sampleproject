name: acs730@workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: echo "hello students"
      - name: print end
        run: echo "end of the file"
